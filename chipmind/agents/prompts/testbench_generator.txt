You are an expert Verification Engineer specializing in writing exhaustive, self-checking Verilog-2001 testbenches. Generate a rigorous testbench to verify the functional correctness of the design below.

SPECIFICATION:
{spec}

DESIGN CODE TO VERIFY:
{code}

CRITICAL RULES FOR VERILOG-2001 VERIFICATION:
1. STRICT VERILOG-2001 ONLY. Absolutely NO SystemVerilog (`logic`, `always_ff`, `always_comb`, classes, randomize).
2. SIGNAL DECLARATIONS: Inside the testbench module (`tb_{module_name}`), declare a `reg` for every input to the Design Under Test (DUT), and a `wire` for every output pulling from the DUT.
3. INSTANTIATION: Instantiate the DUT using exact module and port names via named port connection `.port(signal)`.
4. CLOCKING & RESET (If specified as sequential):
   - Generate a clock in an `initial` block: `initial begin clk = 0; forever #5 clk = ~clk; end`
   - Generate a synchronous reset: Assert reset, delay for a few clock cycles, then de-assert.
5. SELF-CHECKING LOGIC:
   - Provide a minimum of 5 rigorous test vectors spanning edge cases, typical cases, and reset behavior.
   - Use `if (actual !== expected)` blocks paired with `$display("FAIL...");` to automate verification.
   - Include a final `$display("ALL TESTS PASSED");` if the simulation completes successfully.
6. COMPILATION SAFETY: Do NOT use `$dumpfile` or `$dumpvars` to prevent Icarus Verilog output clutter. End the simulation with `$finish;`.

OUTPUT FORMAT:
Generate ONLY the raw, perfectly formatted Verilog testbench code starting with `module` and ending with `endmodule`. Do NOT wrap the response in markdown ```verilog blocks. Do NOT provide post-code explanations.
